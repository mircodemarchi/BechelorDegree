nat r;
nat zeros;
nat ones;

zeros = 0;
ones = 0;

nat i;
i = 0;

nat cnt;
cnt = 10;

do {

    nd(r = 0, r = 1);

    if (r == 0) then {
        zeros = zeros + 1
    }
    else {
        ones = ones + 1
    };

    i = i + 1
} while (i < cnt);

out(zeros);
out(ones)